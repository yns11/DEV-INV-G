/**
 * Types des imports que Vite résout et que TypeScript ignore.
 *
 * Un `import logo from './logo.svg'` est une URL après compilation ; sans cette
 * déclaration, le compilateur ne voit qu'un module introuvable et refuse un
 * import parfaitement valide.
 */

/// <reference types="vite/client" />
